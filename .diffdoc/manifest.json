{
  "lastSyncedCommit": "63ef29fda3e74143a42e4296c609525c5e981b52",
  "files": {
    "src/commands/embed.ts": {
      "hash": "84a5d408dc2f2ca9dd412be97067faa6",
      "summaryText": "**Business Behavior**\nThis script automates the creation and maintenance of a smart, AI-powered search index for code files. It reads an official master list of files, combines each file’s path, AI-generated summary, and code snapshot into a single record, converts those records into machine-readable formats, and stores them in a local search database. The process runs in batches and includes a safety mechanism that rolls back changes if anything fails midway, ensuring the index never ends up in a corrupted state.\n\n**User Impact**\nUsers gain instant, accurate access to code summaries and snapshots through AI search. Instead of manually scanning or searching raw code, they can ask natural language questions and receive precise matches. The system guarantees that only verified, up-to-date files are indexed, and any processing errors are caught before they affect the stored data.\n\n**Rules**\n- Only files explicitly listed in the official manifest are processed.\n- Each file is assigned a permanent identifier (its file path) and a unique fingerprint (hash) to track changes.\n- The index is rebuilt from scratch each time the script runs, ensuring no stale or duplicate records remain.\n- If the manifest contains no files, the system creates an empty index and stops without errors.\n- All updates are grouped into a single transaction; if any step fails, the entire batch is discarded and the previous valid index is preserved.\n\n**Data Movement**\n1. Reads the master manifest file from the project directory.\n2. Extracts file paths, summaries, code snapshots, and hashes.\n3. Combines these elements into structured text blocks.\n4. Sends the text blocks to an AI service to generate numerical representations (embeddings).\n5. Stores the numerical representations alongside the original metadata in a local index folder.\n6. Logs the total number of files successfully indexed.\n\n**Visible Outcomes**\n- A new or refreshed local index folder appears in the project directory.\n- Console output confirms how many files were processed and where the index was saved.\n- Subsequent search or retrieval features can instantly locate and return relevant code summaries and snapshots based on natural language queries.\n- No manual intervention or file tagging is required; the system self-updates whenever the manifest changes.",
      "rawCodeSnapshot": "import fs from \"node:fs/promises\";\nimport path from \"node:path\";\nimport type { RuntimeConfig } from \"../config\";\nimport { LocalIndex, type MetadataTypes } from \"vectra\";\nimport { generateEmbeddings } from \"../utils/llm\";\nimport { getDiffdocBaseDir, resolveDiffdocArtifactPath } from \"../utils/paths\";\nimport type { RepoManifest } from \"./summarize\";\n\nconst VECTRA_INDEX_DIR = \"vectra\";\n\nexport type DiffdocVectorMetadata = Record<string, MetadataTypes> & {\n  filePath: string;\n  hash: string;\n  summaryText: string;\n  rawCodeSnapshot: string;\n};\n\nexport function getVectraIndexPath(config: RuntimeConfig): string {\n  return path.resolve(getDiffdocBaseDir(config.baseDir), VECTRA_INDEX_DIR);\n}\n\nexport interface EmbedOptions {\n  manifest: string;\n}\n\nfunction buildDocument(filePath: string, summaryText: string, rawCodeSnapshot: string): string {\n  return `File: ${filePath}\\n` +\n    `Summary: ${summaryText}\\n\\n` +\n    `Code Snapshot:\\n\\`\\`\\`\\n${rawCodeSnapshot}\\n\\`\\`\\``;\n}\n\nexport async function runEmbed(options: EmbedOptions, config: RuntimeConfig): Promise<void> {\n  const manifestPath = resolveDiffdocArtifactPath(options.manifest, config.baseDir);\n  const manifest = JSON.parse(await fs.readFile(manifestPath, \"utf8\")) as RepoManifest;\n  const entries = Object.entries(manifest.files);\n\n  const indexPath = getVectraIndexPath(config);\n  const index = new LocalIndex<DiffdocVectorMetadata>(indexPath);\n  await index.createIndex({\n    version: 1,\n    deleteIfExists: true,\n    metadata_config: {\n      indexed: [\"filePath\", \"hash\"]\n    }\n  });\n\n  if (entries.length === 0) {\n    console.log(`Created empty Vectra index at ${indexPath}.`);\n    return;\n  }\n\n  const documents = entries.map(([filePath, file]) => buildDocument(filePath, file.summaryText, file.rawCodeSnapshot));\n  const embeddings = await generateEmbeddings(documents, config.embeddings);\n\n  await index.beginUpdate();\n  try {\n    for (let i = 0; i < entries.length; i += 1) {\n      const [filePath, file] = entries[i];\n      await index.upsertItem({\n        id: filePath,\n        vector: embeddings[i],\n        metadata: {\n          filePath,\n          hash: file.hash,\n          summaryText: file.summaryText,\n          rawCodeSnapshot: file.rawCodeSnapshot\n        }\n      });\n    }\n    await index.endUpdate();\n  } catch (error) {\n    index.cancelUpdate();\n    throw error;\n  }\n\n  console.log(`Embedded ${entries.length} summaries into Vectra index at ${indexPath}.`);\n}\n"
    },
    "src/commands/query.ts": {
      "hash": "3684366563fb9098fe178f242359851b",
      "summaryText": "This command enables users to ask natural language questions about code or documentation and receive AI-generated answers backed by stored project history.\n\n**Business Behavior**\nThe system acts as an intelligent search and summarization tool. Instead of manually browsing files or commit history, users can ask questions like \"How was the payment validation updated last sprint?\" The tool locates relevant past changes, synthesizes a clear response, and cites the exact files used. This reduces time spent on code reviews, onboarding, and troubleshooting.\n\n**User Impact**\n- **Faster Information Retrieval:** Users get direct answers instead of sifting through raw code or logs.\n- **Transparency:** Every answer includes a numbered list of source files and relevance scores, allowing users to verify claims and dive deeper if needed.\n- **Flexible Detail Level:** Users can request a quick answer or toggle a detailed view to see full summaries and code snippets.\n\n**Rules & Constraints**\n- **Prerequisite:** The system requires a pre-built search index. If missing, the command stops and instructs the user to run the indexing step first.\n- **Result Limits:** Users specify how many results to retrieve using a `--top` flag. Only positive integers are accepted.\n- **Graceful Handling:** If no relevant data is found, the system clearly states that rather than guessing. If retrieved data is insufficient to answer, the AI explicitly notes what information is missing.\n- **Output Formatting:** Long summaries and code blocks are automatically shortened to fit screen limits, ending with an ellipsis to indicate truncation.\n- **Detail Toggle:** The `--code` flag controls whether detailed summaries and code snapshots are displayed. Without it, only the answer and source list appear.\n\n**Data Movement**\n1. User input is converted into a numerical representation optimized for semantic search.\n2. This representation is matched against a locally stored index of past code changes, summaries, and file metadata.\n3. The top matching results are packaged into a structured prompt and sent to an AI model.\n4. The AI generates a response using only the provided context.\n5. The response, source citations, and optional detailed excerpts are returned to the terminal.\n\n**Visible Outcomes**\n- A concise, direct answer to the user's question.\n- A \"Sources\" section listing matched files with numerical relevance scores.\n- If `--code` is enabled, a detailed breakdown per source showing the file path, score, change hash, a truncated summary, and a truncated code snapshot.\n- Clear error or status messages if the index is missing or no matches are found.",
      "rawCodeSnapshot": "import { LocalIndex } from \"vectra\";\nimport type { RuntimeConfig } from \"../config\";\nimport { generateEmbeddings, promptLlm } from \"../utils/llm\";\nimport { type DiffdocVectorMetadata, getVectraIndexPath } from \"./embed\";\n\nconst CODE_QUERY_PREFIX = \"Represent this query for searching relevant code: \";\n\nexport interface QueryOptions {\n  top: string;\n  code: boolean;\n}\n\nfunction parseTopK(value: string): number {\n  const topK = Number.parseInt(value, 10);\n  if (!Number.isInteger(topK) || topK < 1) {\n    throw new Error(\"Invalid --top value. Expected a positive integer.\");\n  }\n\n  return topK;\n}\n\nfunction trimForDisplay(text: string, maxLength: number): string {\n  if (text.length <= maxLength) {\n    return text;\n  }\n\n  return `${text.slice(0, maxLength).trimEnd()}...`;\n}\n\nfunction buildAnswerPrompt(question: string, results: Awaited<ReturnType<LocalIndex<DiffdocVectorMetadata>[\"queryItems\"]>>): string {\n  const context = results.map((result, indexPosition) => {\n    const metadata = result.item.metadata;\n    return [\n      `Result ${indexPosition + 1}`,\n      `File: ${metadata.filePath}`,\n      `Score: ${result.score}`,\n      `Summary:\\n${metadata.summaryText}`,\n      `Code Snapshot:\\n${metadata.rawCodeSnapshot}`\n    ].join(\"\\n\");\n  }).join(\"\\n\\n---\\n\\n\");\n\n  return `Answer the user's question using only the retrieved DiffDoc results below. If the results do not contain enough information, say what is missing. Prefer a direct answer first, then cite the relevant file paths. Keep the explanation appropriate to the question: summarize when asked for a summary, explain implementation details when asked how something works, and avoid unsupported claims.\\n\\nUser question:\\n${question}\\n\\nRetrieved results:\\n${context}`;\n}\n\nexport async function runQuery(message: string, options: QueryOptions, config: RuntimeConfig): Promise<void> {\n  const topK = parseTopK(options.top);\n  const indexPath = getVectraIndexPath(config);\n  const index = new LocalIndex<DiffdocVectorMetadata>(indexPath);\n\n  if (!await index.isIndexCreated()) {\n    throw new Error(`No Vectra index found at ${indexPath}. Run \"diffdoc embed\" first.`);\n  }\n\n  const [queryVector] = await generateEmbeddings([`${CODE_QUERY_PREFIX}${message}`], config.embeddings);\n  const results = await index.queryItems(queryVector, message, topK);\n\n  if (results.length === 0) {\n    console.log(\"No matching embedded summaries found.\");\n    return;\n  }\n\n  const answer = await promptLlm(buildAnswerPrompt(message, results), config.chat);\n  console.log(answer);\n\n  console.log(\"\\nSources:\");\n  for (const [indexPosition, result] of results.entries()) {\n    const metadata = result.item.metadata;\n    console.log(`${indexPosition + 1}. ${metadata.filePath} (${result.score.toFixed(4)})`);\n  }\n\n  if (!options.code) {\n    return;\n  }\n\n  for (const [indexPosition, result] of results.entries()) {\n    const metadata = result.item.metadata;\n    console.log(`\\n#${indexPosition + 1} ${metadata.filePath}`);\n    console.log(`Score: ${result.score.toFixed(4)}`);\n    console.log(`Hash: ${metadata.hash}`);\n    console.log(\"Summary:\");\n    console.log(trimForDisplay(metadata.summaryText, 1200));\n\n    if (options.code) {\n      console.log(\"Code Snapshot:\");\n      console.log(trimForDisplay(metadata.rawCodeSnapshot, 2000));\n    }\n  }\n}\n"
    },
    "src/commands/summarize.ts": {
      "hash": "70117c82193017b3059042d496babb52",
      "summaryText": "This tool automatically tracks and documents code changes in a software project. It scans source code files, generates plain-English descriptions of what each file does, and saves those descriptions in a structured record that updates over time.\n\n**Business Behavior**\nThe system maintains a living inventory of the codebase. It can either rebuild the entire inventory from scratch or only update the parts that have changed since the last run. This ensures the documentation stays current without requiring manual intervention or full reprocessing every time.\n\n**User Impact**\nDevelopers and project managers get an up-to-date, machine-readable summary of the codebase without writing documentation manually. Teams can quickly understand what specific files do, track how the project evolves, and onboard new members faster. The tool reduces administrative overhead and keeps technical knowledge synchronized with actual code.\n\n**Rules & Constraints**\n- Only processes files with specific programming extensions: `.ts`, `.js`, `.cs`, and `.py`.\n- Automatically skips dependency folders, build outputs, and version control metadata.\n- Ignores lock files that track package versions.\n- Uses a content fingerprint to detect changes. If a file's content hasn't changed since the last run, it is skipped to save time and processing costs.\n- Tracks the exact project state (commit) last processed to ensure accurate change detection.\n\n**Data Movement**\n1. Reads target code files from the project directory.\n2. Sends file contents to an AI service to generate functional summaries.\n3. Stores each file's path, content fingerprint, generated summary, and original code in a structured JSON record.\n4. Updates the record's timestamp to reflect the current project state.\n5. Saves the updated record to a designated output location.\n\n**Visible Outcomes**\n- A continuously updated manifest file containing summaries of all tracked code files.\n- Console messages confirming which files were summarized, updated, or removed.\n- Accurate change tracking that reflects the exact state of the repository at the time of the last run.\n- Efficient processing that avoids redundant work by only touching modified or new files.",
      "rawCodeSnapshot": "import fs from \"node:fs/promises\";\nimport path from \"node:path\";\nimport type { RuntimeConfig } from \"../config\";\nimport { getCurrentCommit, getGitDeltas } from \"../utils/git\";\nimport { hashFileContent } from \"../utils/hashing\";\nimport { generateFunctionalSummary } from \"../utils/llm\";\nimport { resolveDiffdocArtifactPath } from \"../utils/paths\";\n\nconst TARGET_EXTENSIONS = new Set([\".ts\", \".js\", \".cs\", \".py\"]);\nconst IGNORED_DIRECTORIES = new Set([\".git\", \"node_modules\", \"dist\"]);\nconst IGNORED_FILES = new Set([\"package-lock.json\", \"yarn.lock\", \"pnpm-lock.yaml\", \"bun.lockb\"]);\n\nexport interface ManifestFileEntry {\n  hash: string;\n  summaryText: string;\n  rawCodeSnapshot: string;\n}\n\nexport interface RepoManifest {\n  lastSyncedCommit: string;\n  files: Record<string, ManifestFileEntry>;\n}\n\nexport interface SummarizeOptions {\n  path: string;\n  out: string;\n  mode: \"all\" | \"delta\";\n}\n\nfunction normalizeRelativePath(filePath: string): string {\n  return filePath.split(path.sep).join(\"/\");\n}\n\nfunction isTargetCodeFile(filePath: string): boolean {\n  return TARGET_EXTENSIONS.has(path.extname(filePath)) && !IGNORED_FILES.has(path.basename(filePath));\n}\n\nasync function readManifest(manifestPath: string): Promise<RepoManifest> {\n  try {\n    return JSON.parse(await fs.readFile(manifestPath, \"utf8\")) as RepoManifest;\n  } catch (error) {\n    const nodeError = error as NodeJS.ErrnoException;\n    if (nodeError.code === \"ENOENT\") {\n      return { lastSyncedCommit: \"\", files: {} };\n    }\n    throw error;\n  }\n}\n\nasync function writeManifest(manifestPath: string, manifest: RepoManifest): Promise<void> {\n  await fs.mkdir(path.dirname(manifestPath), { recursive: true });\n  await fs.writeFile(manifestPath, `${JSON.stringify(manifest, null, 2)}\\n`, \"utf8\");\n}\n\nasync function walkCodeFiles(rootPath: string, currentPath = rootPath): Promise<string[]> {\n  const entries = await fs.readdir(currentPath, { withFileTypes: true });\n  const files: string[] = [];\n\n  for (const entry of entries) {\n    const entryPath = path.join(currentPath, entry.name);\n    if (entry.isDirectory()) {\n      if (!IGNORED_DIRECTORIES.has(entry.name)) {\n        files.push(...await walkCodeFiles(rootPath, entryPath));\n      }\n      continue;\n    }\n\n    if (entry.isFile() && isTargetCodeFile(entry.name)) {\n      files.push(normalizeRelativePath(path.relative(rootPath, entryPath)));\n    }\n  }\n\n  return files.sort();\n}\n\nasync function summarizeFile(rootPath: string, relativePath: string, config: RuntimeConfig): Promise<ManifestFileEntry> {\n  const absolutePath = path.join(rootPath, relativePath);\n  const rawCodeSnapshot = await fs.readFile(absolutePath, \"utf8\");\n  return {\n    hash: hashFileContent(rawCodeSnapshot),\n    summaryText: await generateFunctionalSummary(relativePath, rawCodeSnapshot, config.chat),\n    rawCodeSnapshot\n  };\n}\n\nexport async function runSummarize(options: SummarizeOptions, config: RuntimeConfig): Promise<void> {\n  if (options.mode !== \"all\" && options.mode !== \"delta\") {\n    throw new Error('Invalid summarize mode. Expected \"all\" or \"delta\".');\n  }\n\n  const commandCwd = process.cwd();\n  const repoPath = path.resolve(commandCwd, options.path);\n  const manifestPath = resolveDiffdocArtifactPath(options.out, config.baseDir);\n  const manifest = options.mode === \"delta\" ? await readManifest(manifestPath) : { lastSyncedCommit: \"\", files: {} };\n\n  if (options.mode === \"all\") {\n    const files = await walkCodeFiles(repoPath);\n    manifest.files = {};\n    for (const filePath of files) {\n      manifest.files[filePath] = await summarizeFile(repoPath, filePath, config);\n      console.log(`Summarized ${filePath}`);\n    }\n  } else {\n    const deltas = await getGitDeltas(repoPath, manifest.lastSyncedCommit);\n    for (const deletedPath of deltas.deleted) {\n      delete manifest.files[deletedPath];\n      console.log(`Pruned ${deletedPath}`);\n    }\n\n    for (const filePath of deltas.modifiedOrAdded) {\n      const absolutePath = path.join(repoPath, filePath);\n      try {\n        const rawCodeSnapshot = await fs.readFile(absolutePath, \"utf8\");\n        const hash = hashFileContent(rawCodeSnapshot);\n        if (manifest.files[filePath]?.hash === hash) continue;\n        manifest.files[filePath] = {\n          hash,\n          summaryText: await generateFunctionalSummary(filePath, rawCodeSnapshot, config.chat),\n          rawCodeSnapshot\n        };\n        console.log(`Updated ${filePath}`);\n      } catch (error) {\n        const nodeError = error as NodeJS.ErrnoException;\n        if (nodeError.code === \"ENOENT\") {\n          delete manifest.files[filePath];\n          continue;\n        }\n        throw error;\n      }\n    }\n  }\n\n  manifest.lastSyncedCommit = await getCurrentCommit(repoPath);\n  await writeManifest(manifestPath, manifest);\n  console.log(`Wrote manifest to ${manifestPath}`);\n}\n"
    },
    "src/index.ts": {
      "hash": "330c6a36dddf3c59d8c3131ce5a6b49b",
      "summaryText": "**Business Behavior**\nThis tool automatically converts technical code updates into plain-English business summaries. It scans a project folder, generates structured records of what changed, stores those records in a searchable local database, and allows users to ask natural-language questions about the system. It also supports direct, on-demand conversations with an AI assistant for quick clarifications.\n\n**User Impact**\n- **Faster Onboarding & Review:** Teams can quickly understand recent code changes without reading raw technical files.\n- **Self-Service Information:** Business analysts, product managers, or developers can ask questions like “What does the payment module do?” and receive targeted answers with supporting context.\n- **Structured Documentation:** The tool produces a portable summary file that can be shared, archived, or integrated into other business workflows.\n\n**Rules & Configuration**\n- Users choose between running AI locally (private, offline) or via a cloud provider (requires an API key).\n- Users specify which folder to scan, where to save results, and whether to summarize all code or only recent changes.\n- Search results can be limited to a specific number of matches, and users can choose whether to include raw code snippets in the output.\n- If a required setting is missing, the tool falls back to environment variables or defaults to standard locations.\n\n**Data Movement**\n1. Reads source code from a specified directory.\n2. Sends code details to an AI service to generate plain-English summaries.\n3. Saves those summaries into a JSON file.\n4. Converts the summaries into a format suitable for fast searching and stores them in a local index.\n5. When a user asks a question, the tool searches the index, retrieves the most relevant summaries, and returns them as a response.\n\n**Visible Outcomes**\n- A JSON file containing structured, business-readable summaries of code changes.\n- Direct text responses to natural-language questions about the codebase.\n- Search results showing the most relevant code summaries, optionally including code snippets.\n- Clear error messages if configuration is missing, paths are invalid, or AI services are unreachable.",
      "rawCodeSnapshot": "#!/usr/bin/env node\nimport { Command } from \"commander\";\nimport { buildRuntimeConfig, type RuntimeConfigOptions } from \"./config\";\nimport { runEmbed } from \"./commands/embed\";\nimport { runQuery } from \"./commands/query\";\nimport { runSummarize } from \"./commands/summarize\";\nimport { promptLlm } from \"./utils/llm\";\n\nconst program = new Command();\n\nfunction addBaseOptions(command: Command): Command {\n  return command\n    .option(\"--config <path>\", \"path to .diffdocrc JSON config file\")\n    .option(\"--base-dir <path>\", \"DiffDoc artifact directory\")\n    .option(\"--ai-provider <provider>\", \"AI provider: local or cloud\");\n}\n\nfunction addChatOptions(command: Command): Command {\n  return command\n    .option(\"--local-llm-endpoint <url>\", \"local OpenAI-compatible chat endpoint\")\n    .option(\"--local-chat-model <model>\", \"local chat model name\")\n    .option(\"--cloud-llm-endpoint <url>\", \"cloud OpenAI-compatible chat endpoint\")\n    .option(\"--cloud-chat-model <model>\", \"cloud chat model name\")\n    .option(\"--openai-api-key <key>\", \"OpenAI-compatible API key; falls back to OPENAI_API_KEY\");\n}\n\nfunction addEmbeddingOptions(command: Command): Command {\n  return command\n    .option(\"--local-embed-endpoint <url>\", \"local OpenAI-compatible embeddings endpoint\")\n    .option(\"--local-embed-model <model>\", \"local embedding model name\")\n    .option(\"--cloud-embed-model <model>\", \"cloud embedding model name\");\n}\n\nfunction addCloudEndpointAndKeyOptions(command: Command): Command {\n  return command\n    .option(\"--cloud-llm-endpoint <url>\", \"cloud OpenAI-compatible endpoint\")\n    .option(\"--openai-api-key <key>\", \"OpenAI-compatible API key; falls back to OPENAI_API_KEY\");\n}\n\nprogram\n  .name(\"diffdoc\")\n  .description(\"Translate repository code shifts into plain-English business context\")\n  .version(\"0.1.0\");\n\naddChatOptions(addBaseOptions(program\n  .command(\"summarize\")))\n  .description(\"Summarize repository code into a portable JSON manifest\")\n  .option(\"--path <path>\", \"repository or code path to scan\", \".\")\n  .option(\"--out <path>\", \"manifest output path under --base-dir\", \"manifest.json\")\n  .option(\"--mode <mode>\", \"summarization mode: all or delta\", \"all\")\n  .action(async (options: RuntimeConfigOptions & { path: string; out: string; mode: string }) => {\n    try {\n      const config = buildRuntimeConfig(options, { chat: true });\n      await runSummarize({ path: options.path, out: options.out, mode: options.mode as \"all\" | \"delta\" }, config);\n    } catch (error) {\n      console.error(error instanceof Error ? error.message : error);\n      process.exit(1);\n    }\n  });\n\naddChatOptions(addBaseOptions(program\n  .command(\"prompt\")))\n  .description(\"Send a plain prompt to the configured LLM\")\n  .argument(\"<message...>\", \"prompt text to send\")\n  .action(async (messageParts: string[], options: RuntimeConfigOptions) => {\n    try {\n      const config = buildRuntimeConfig(options, { chat: true });\n      const response = await promptLlm(messageParts.join(\" \"), config.chat);\n      console.log(response);\n    } catch (error) {\n      console.error(error instanceof Error ? error.message : error);\n      process.exit(1);\n    }\n  });\n\naddEmbeddingOptions(addChatOptions(addBaseOptions(program\n  .command(\"query\"))))\n  .description(\"Search the local Vectra index with a natural-language question\")\n  .argument(\"<message...>\", \"question or search text\")\n  .option(\"--top <count>\", \"number of matches to return\", \"5\")\n  .option(\"--code\", \"include code snapshots in results\", false)\n  .action(async (messageParts: string[], options: RuntimeConfigOptions & { top: string; code: boolean }) => {\n    try {\n      const config = buildRuntimeConfig(options, { chat: true, embeddings: true });\n      await runQuery(messageParts.join(\" \"), options, config);\n    } catch (error) {\n      console.error(error instanceof Error ? error.message : error);\n      process.exit(1);\n    }\n  });\n\naddCloudEndpointAndKeyOptions(addEmbeddingOptions(addBaseOptions(program\n  .command(\"embed\")))\n)\n  .description(\"Embed manifest summaries into a local Vectra index\")\n  .option(\"--manifest <path>\", \"manifest input path under --base-dir\", \"manifest.json\")\n  .action(async (options: RuntimeConfigOptions & { manifest: string }) => {\n    try {\n      const config = buildRuntimeConfig(options, { embeddings: true });\n      await runEmbed({ manifest: options.manifest }, config);\n    } catch (error) {\n      console.error(error instanceof Error ? error.message : error);\n      process.exit(1);\n    }\n  });\n\nprogram.parseAsync(process.argv).catch((error: unknown) => {\n  console.error(error instanceof Error ? error.message : error);\n  process.exit(1);\n});\n"
    },
    "src/utils/git.ts": {
      "hash": "c1f79aa1f6c9df51373901a7e79cbd2f",
      "summaryText": "**Business Purpose & Behavior**\nThis module scans a software project folder to identify which programming files have changed since a specific checkpoint. It serves as a change-tracking filter that tells downstream systems exactly which parts of the codebase are new, updated, or removed, so the business can decide what to build, test, or deploy next.\n\n**Rules & Filtering**\n- Only tracks files written in four specific languages: TypeScript, JavaScript, C#, and Python.\n- Ignores all other file types (documents, images, configuration files, etc.).\n- Combines two sources of changes: files already saved to version history since a chosen reference point, plus any unsaved changes currently in the working folder.\n- Removes duplicates and standardizes file paths to a consistent format.\n- Returns results in alphabetical order for predictable processing.\n\n**Data Movement**\n- Reads file status directly from the project's version control folder.\n- Outputs two clean lists: one for files that were added or modified, and one for files that were deleted.\n- If no saved version exists yet, it defaults to tracking only the current unsaved changes.\n\n**User & System Impact**\n- Prevents unnecessary work by ensuring only relevant code files trigger downstream processes like automated testing, code reviews, or deployment pipelines.\n- Provides a reliable, up-to-date snapshot of code changes without requiring manual file tracking.\n- Returns a simple \"uncommitted\" status when the project has no saved history, allowing systems to handle fresh or incomplete work gracefully.\n\n**Visible Outcomes**\n- A structured report listing exactly which programming files changed or were removed.\n- Consistent, duplicate-free, and sorted file paths ready for integration with other business tools or automated workflows.",
      "rawCodeSnapshot": "import path from \"node:path\";\nimport simpleGit from \"simple-git\";\n\nconst TARGET_EXTENSIONS = new Set([\".ts\", \".js\", \".cs\", \".py\"]);\n\nexport interface GitDeltas {\n  modifiedOrAdded: string[];\n  deleted: string[];\n}\n\nfunction normalizePath(filePath: string): string {\n  return filePath.split(path.sep).join(\"/\");\n}\n\nfunction isTargetCodeFile(filePath: string): boolean {\n  return TARGET_EXTENSIONS.has(path.extname(filePath));\n}\n\nfunction addUnique(target: Set<string>, filePath: string): void {\n  const normalized = normalizePath(filePath.trim());\n  if (normalized && isTargetCodeFile(normalized)) {\n    target.add(normalized);\n  }\n}\n\nexport async function getGitDeltas(repoPath: string, sinceRef: string): Promise<GitDeltas> {\n  const git = simpleGit(repoPath);\n  const modifiedOrAdded = new Set<string>();\n  const deleted = new Set<string>();\n\n  if (sinceRef) {\n    const output = await git.raw([\"diff\", \"--name-status\", `${sinceRef}..HEAD`]);\n    for (const line of output.split(/\\r?\\n/)) {\n      if (!line.trim()) continue;\n      const [status, ...rest] = line.split(/\\s+/);\n      const filePath = rest[rest.length - 1];\n      if (status.startsWith(\"D\")) {\n        addUnique(deleted, filePath);\n      } else {\n        addUnique(modifiedOrAdded, filePath);\n      }\n    }\n  }\n\n  const status = await git.status();\n  for (const filePath of [...status.created, ...status.modified, ...status.renamed.map((item) => item.to)]) {\n    addUnique(modifiedOrAdded, filePath);\n  }\n  for (const filePath of status.deleted) {\n    addUnique(deleted, filePath);\n  }\n\n  return {\n    modifiedOrAdded: [...modifiedOrAdded].sort(),\n    deleted: [...deleted].sort()\n  };\n}\n\nexport async function getCurrentCommit(repoPath: string): Promise<string> {\n  const git = simpleGit(repoPath);\n  try {\n    return (await git.revparse([\"HEAD\"])).trim();\n  } catch {\n    return \"uncommitted\";\n  }\n}\n"
    },
    "src/utils/hashing.ts": {
      "hash": "b16f8d533a9ec0d3408738f1c06f6fa1",
      "summaryText": "This code generates a unique digital fingerprint for any text or file content. It takes the raw content, runs it through a standardized conversion process, and outputs a fixed-length string of letters and numbers that represents that content.\n\n**Business Behavior**\nThe system uses this fingerprint to quickly identify files, detect duplicates, and verify that content hasn't changed. Instead of storing or comparing entire files, the application compares these short fingerprints, which saves storage space and speeds up processing.\n\n**User Impact**\nUsers experience faster uploads, automatic duplicate detection, and reliable version tracking. The system can instantly alert users if they are uploading a file that already exists or if a previously saved file has been modified, reducing errors and manual verification work.\n\n**Rules**\n- The exact same content will always produce the exact same fingerprint.\n- Even a single character change will produce a completely different fingerprint.\n- The fingerprint is always the same length and format, regardless of how large or small the original content is.\n- The process is one-way: the fingerprint cannot be converted back into the original content.\n\n**Data Movement**\nThe original file content is read into memory, processed to create the fingerprint, and then the original content is typically released or stored separately. Only the generated fingerprint is saved to the database or passed to other parts of the system for comparison and tracking.\n\n**Visible Outcomes**\nUsers will see system messages such as \"File already exists,\" \"Content updated,\" or \"Duplicate detected.\" The fingerprint generation itself runs silently in the background and is not directly visible to users, but it powers these automated checks and status updates.",
      "rawCodeSnapshot": "import { createHash } from \"node:crypto\";\n\nexport function hashFileContent(fileContent: string): string {\n  return createHash(\"md5\").update(fileContent, \"utf8\").digest(\"hex\");\n}\n"
    },
    "src/utils/llm.ts": {
      "hash": "fbd633126523c05fffae7f6c53c59cfa",
      "summaryText": "This module connects the application to an external AI service to process text and code. It provides three distinct capabilities:\n\n**1. Plain-English Code Summaries**\n- **Business Behavior:** Takes raw code and a file name, sends it to the AI, and returns a concise explanation written for non-technical readers.\n- **Rules:** The AI is strictly instructed to ignore technical details and focus only on business impact, user-facing behavior, internal rules, data flow, and final outcomes. It uses a low randomness setting to ensure consistent, predictable outputs.\n- **Data Movement:** Code content is sent securely to the AI provider. The provider returns a text summary, which the application displays or stores.\n- **Visible Outcome:** Stakeholders receive a clear, jargon-free description of what a piece of code does, enabling better decision-making without needing developer involvement.\n\n**2. General AI Question & Answer**\n- **Business Behavior:** Accepts any user-submitted question or instruction and returns the AI’s direct response.\n- **Rules:** Passes the input exactly as provided to the AI model with a low randomness setting for reliability.\n- **Data Movement:** User text is sent to the AI provider. The generated response is returned to the application.\n- **Visible Outcome:** Users or internal teams can ask the system questions and receive AI-generated answers, supporting tasks like drafting content, answering policy questions, or generating ideas.\n\n**3. Text-to-Number Conversion (Embeddings)**\n- **Business Behavior:** Converts a list of text strings into numerical representations that capture their meaning.\n- **Rules:** Sends all provided texts to the AI provider in a single batch. The provider returns a structured list of numbers for each text.\n- **Data Movement:** Raw text is transmitted to the AI service. The service returns numerical arrays, which the application uses internally.\n- **Visible Outcome:** Enables the system to find, group, or rank content by meaning rather than exact keywords. This powers features like smart search, content recommendations, or duplicate detection.\n\n**Shared Operational Notes**\n- All interactions use a configured API key and endpoint, ensuring secure and controlled access to the AI service.\n- If the AI service fails or returns empty data, the system safely defaults to a clear placeholder message instead of crashing.\n- The low randomness setting across all functions prioritizes accuracy and consistency over creative variation.",
      "rawCodeSnapshot": "import OpenAI from \"openai\";\nimport type { ChatConfig, EmbeddingConfig } from \"../config\";\n\nfunction createClient(config: ChatConfig): { client: OpenAI; model: string } {\n  return {\n    client: new OpenAI({ apiKey: config.apiKey, baseURL: config.baseURL }),\n    model: config.model\n  };\n}\n\nexport async function generateFunctionalSummary(fileName: string, codeContent: string, config: ChatConfig): Promise<string> {\n  const { client, model } = createClient(config);\n  const response = await client.chat.completions.create({\n    model,\n    temperature: 0.2,\n    messages: [\n      {\n        role: \"system\",\n        content: \"Explain what this code does for non-technical stakeholders. Focus on business behavior, user impact, rules, data movement, and visible outcomes. Use plain English, avoid jargon, and provide zero conversational preamble.\"\n      },\n      {\n        role: \"user\",\n        content: `File: ${fileName}\\n\\nCode:\\n${codeContent}`\n      }\n    ]\n  });\n\n  return response.choices[0]?.message?.content?.trim() || \"No business behavior summary was returned.\";\n}\n\nexport async function promptLlm(prompt: string, config: ChatConfig): Promise<string> {\n  const { client, model } = createClient(config);\n  const response = await client.chat.completions.create({\n    model,\n    temperature: 0.2,\n    messages: [\n      {\n        role: \"user\",\n        content: prompt\n      }\n    ]\n  });\n\n  return response.choices[0]?.message?.content?.trim() || \"No response was returned.\";\n}\n\nexport async function generateEmbeddings(texts: string[], config: EmbeddingConfig): Promise<number[][]> {\n  const baseURL = config.baseURL.replace(/\\/embeddings\\/?$/, \"\");\n  const client = new OpenAI({ apiKey: config.apiKey, baseURL });\n  const response = await client.embeddings.create({\n    model: config.model,\n    input: texts\n  });\n\n  return response.data.map((item) => item.embedding);\n}\n"
    },
    "src/utils/paths.ts": {
      "hash": "9761e45294e4c3b6c226a3e0a770362c",
      "summaryText": "This code manages how the system locates and organizes documentation files and related assets. It acts as a reliable address-finding tool that ensures the program always knows exactly where to look for or save files.\n\n**Business Behavior & Rules**\n- The system uses a designated \"main folder\" as the starting point for all file operations.\n- If a file location is already complete (starts from the root of the computer), the system uses it exactly as provided.\n- If a file location is short or relative (like just a folder name or a partial path), the system automatically attaches it to the main folder to create a complete, unambiguous address.\n- The main folder's location is always calculated from where the program is currently running, ensuring consistency regardless of where the software is installed or executed.\n\n**Data Movement**\n- The code does not move or copy data itself. It only calculates the correct destination or source addresses so other parts of the system can read, write, or generate documentation files accurately.\n\n**User Impact**\n- Users can reference files using simple, short names or relative paths instead of complex, full system addresses.\n- The system prevents common errors like \"file not found\" or \"invalid path\" by automatically resolving locations behind the scenes.\n- Documentation generation or review workflows run smoothly without manual path configuration.\n\n**Visible Outcomes**\n- Consistent, predictable file organization across different environments.\n- Fewer broken references or missing assets during documentation builds.\n- A cleaner, more reliable user experience when generating, updating, or accessing project documentation.",
      "rawCodeSnapshot": "import path from \"node:path\";\n\nexport function getDiffdocBaseDir(baseDir: string): string {\n  return path.resolve(process.cwd(), baseDir);\n}\n\nexport function resolveDiffdocArtifactPath(filePath: string, baseDir: string): string {\n  if (path.isAbsolute(filePath)) {\n    return filePath;\n  }\n\n  return path.resolve(getDiffdocBaseDir(baseDir), filePath);\n}\n"
    },
    "src/config.ts": {
      "hash": "3025480f2eb8ac34509bdca9a6dc1d28",
      "summaryText": "This code sets up and validates the settings the application needs to connect to artificial intelligence services before any work begins. It acts as a configuration manager that ensures the system knows exactly which AI provider to use, where to connect, which models to run, and how to authenticate.\n\n**Business Behavior**\nThe system supports two deployment modes: running AI locally on your own hardware or using a cloud-based AI service. This configuration module automatically selects the correct mode, loads the necessary connection details, and verifies that all required information is present. If critical settings are missing, it stops the application immediately rather than failing later during actual use.\n\n**User Impact**\nUsers can configure the application in three ways, with command-line settings taking priority over a saved configuration file, which in turn overrides system environment variables. This flexibility allows developers to set up defaults in a file, override them temporarily for testing, or secure sensitive keys in the operating system. If settings are incomplete, the user receives a clear error message pointing to exactly what is missing, preventing wasted time debugging broken runs.\n\n**Rules**\n- Provider selection is strictly limited to \"local\" or \"cloud\".\n- Configuration priority: runtime inputs > `.diffdocrc` file > environment variables > built-in defaults.\n- Cloud mode requires a valid API key. Local mode does not.\n- Chat and embedding features are optional but mandatory if requested. Each requires a service endpoint and a specific model name.\n- The system enforces strict validation: if a required endpoint, model, or API key is missing, it throws a descriptive error and halts execution.\n\n**Data Movement**\nThis module only reads and structures configuration data. It pulls settings from a JSON file, system environment variables, and runtime inputs, then outputs a single, validated configuration object. No user documents, AI prompts, or model data pass through this code. It purely prepares the connection parameters for the rest of the application.\n\n**Visible Outcomes**\n- A ready-to-use configuration object that tells the application which AI service to contact, which models to load, and how to authenticate.\n- Immediate, clear error messages if configuration is incomplete or invalid.\n- Consistent behavior across environments, ensuring the application always runs with the intended AI provider and model settings.",
      "rawCodeSnapshot": "import fs from \"node:fs\";\nimport path from \"node:path\";\n\nexport type AiProvider = \"local\" | \"cloud\";\n\nexport interface ChatConfig {\n  apiKey: string;\n  baseURL: string;\n  model: string;\n}\n\nexport interface EmbeddingConfig {\n  apiKey: string;\n  baseURL: string;\n  model: string;\n}\n\nexport interface RuntimeConfig {\n  baseDir: string;\n  provider: AiProvider;\n  chat: ChatConfig;\n  embeddings: EmbeddingConfig;\n}\n\nexport interface RuntimeConfigOptions {\n  config?: string;\n  baseDir?: string;\n  aiProvider?: string;\n  localLlmEndpoint?: string;\n  localEmbedEndpoint?: string;\n  localChatModel?: string;\n  localEmbedModel?: string;\n  cloudLlmEndpoint?: string;\n  cloudChatModel?: string;\n  cloudEmbedModel?: string;\n  openaiApiKey?: string;\n}\n\nexport interface RuntimeConfigNeeds {\n  chat?: boolean;\n  embeddings?: boolean;\n}\n\nfunction readOption(value: string | undefined, envName: string, fallback = \"\"): string {\n  return value || process.env[envName] || fallback;\n}\n\nfunction loadRcFile(configPath: string | undefined): RuntimeConfigOptions {\n  const resolvedPath = path.resolve(process.cwd(), configPath || \".diffdocrc\");\n  if (!fs.existsSync(resolvedPath)) {\n    if (configPath) {\n      throw new Error(`Config file not found: ${resolvedPath}`);\n    }\n    return {};\n  }\n\n  const parsed = JSON.parse(fs.readFileSync(resolvedPath, \"utf8\")) as unknown;\n  if (!parsed || typeof parsed !== \"object\" || Array.isArray(parsed)) {\n    throw new Error(`Config file must contain a JSON object: ${resolvedPath}`);\n  }\n\n  return parsed as RuntimeConfigOptions;\n}\n\nfunction mergeConfigOptions(options: RuntimeConfigOptions): RuntimeConfigOptions {\n  const rcOptions = loadRcFile(options.config);\n  return {\n    ...rcOptions,\n    ...options\n  };\n}\n\nfunction readProvider(value: string | undefined): AiProvider {\n  const provider = readOption(value, \"AI_PROVIDER\", \"local\");\n  if (provider !== \"local\" && provider !== \"cloud\") {\n    throw new Error('Invalid AI provider. Expected \"local\" or \"cloud\".');\n  }\n\n  return provider;\n}\n\nexport function buildRuntimeConfig(options: RuntimeConfigOptions, needs: RuntimeConfigNeeds = { chat: true, embeddings: true }): RuntimeConfig {\n  const mergedOptions = mergeConfigOptions(options);\n  const provider = readProvider(mergedOptions.aiProvider);\n  const apiKey = readOption(mergedOptions.openaiApiKey, \"OPENAI_API_KEY\", provider === \"local\" ? \"local-key\" : \"\");\n\n  const chatBaseURL = provider === \"cloud\"\n    ? readOption(mergedOptions.cloudLlmEndpoint, \"CLOUD_LLM_ENDPOINT\", \"https://api.openai.com/v1\")\n    : readOption(mergedOptions.localLlmEndpoint, \"LOCAL_LLM_ENDPOINT\");\n  const chatModel = provider === \"cloud\"\n    ? readOption(mergedOptions.cloudChatModel, \"CLOUD_CHAT_MODEL\", \"gpt-4o-mini\")\n    : readOption(mergedOptions.localChatModel, \"LOCAL_CHAT_MODEL\");\n  const embedBaseURL = provider === \"cloud\"\n    ? readOption(mergedOptions.cloudLlmEndpoint, \"CLOUD_LLM_ENDPOINT\", \"https://api.openai.com/v1\")\n    : readOption(mergedOptions.localEmbedEndpoint, \"LOCAL_EMBED_ENDPOINT\");\n  const embedModel = provider === \"cloud\"\n    ? readOption(mergedOptions.cloudEmbedModel, \"CLOUD_EMBED_MODEL\", \"text-embedding-3-small\")\n    : readOption(mergedOptions.localEmbedModel, \"LOCAL_EMBED_MODEL\");\n\n  if (needs.chat && !chatBaseURL) {\n    throw new Error(`Missing ${provider === \"cloud\" ? \"cloud\" : \"local\"} chat endpoint. Pass the runtime option or set ${provider === \"cloud\" ? \"CLOUD_LLM_ENDPOINT\" : \"LOCAL_LLM_ENDPOINT\"}.`);\n  }\n  if (needs.chat && !chatModel) {\n    throw new Error(`Missing ${provider === \"cloud\" ? \"cloud\" : \"local\"} chat model. Pass the runtime option or set ${provider === \"cloud\" ? \"CLOUD_CHAT_MODEL\" : \"LOCAL_CHAT_MODEL\"}.`);\n  }\n  if (needs.embeddings && !embedBaseURL) {\n    throw new Error(`Missing ${provider === \"cloud\" ? \"cloud\" : \"local\"} embedding endpoint. Pass the runtime option or set ${provider === \"cloud\" ? \"CLOUD_LLM_ENDPOINT\" : \"LOCAL_EMBED_ENDPOINT\"}.`);\n  }\n  if (needs.embeddings && !embedModel) {\n    throw new Error(`Missing ${provider === \"cloud\" ? \"cloud\" : \"local\"} embedding model. Pass the runtime option or set ${provider === \"cloud\" ? \"CLOUD_EMBED_MODEL\" : \"LOCAL_EMBED_MODEL\"}.`);\n  }\n  if (provider === \"cloud\" && (needs.chat || needs.embeddings) && !apiKey) {\n    throw new Error(\"Missing OpenAI API key. Pass --openai-api-key or set OPENAI_API_KEY.\");\n  }\n\n  return {\n    baseDir: readOption(mergedOptions.baseDir, \"DIFFDOC_BASE_DIR\", \"./.diffdoc\"),\n    provider,\n    chat: {\n      apiKey,\n      baseURL: chatBaseURL,\n      model: chatModel\n    },\n    embeddings: {\n      apiKey,\n      baseURL: embedBaseURL,\n      model: embedModel\n    }\n  };\n}\n"
    }
  }
}
